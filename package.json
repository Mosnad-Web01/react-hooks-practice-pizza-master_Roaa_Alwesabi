{
  "name": "react-hooks-practice-pizza",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.7.7",
    "bootstrap": "^4.1.3",
    "browserify-zlib": "^0.2.0",
    "https-browserify": "^1.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "^5.0.1",
    "stream-browserify": "^3.0.0",
    "url": "^0.11.4"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "server": "json-server --watch db.json -p 3001",
    "test": "jest"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
